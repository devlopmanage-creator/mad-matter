---
// No props needed for this component
---

<section id="home">
  <!-- Background with optimized loading -->
  <div class="background">
    <img
      class="background-placeholder"
      src="https://res.cloudinary.com/doleg7smw/image/upload/c_scale,w_1920,q_auto,f_auto/v1758771282/photo_2025-09-25_00-14-17_kspwix.jpg"
      alt="Building materials showcase"
      loading="eager"
      decoding="async"
      width="1920"
      height="1080"
    />
    <video
      class="background-video"
      src="https://res.cloudinary.com/doleg7smw/video/upload/c_scale,w_1920,q_auto,f_auto/v1758669242/Clip_1_kxpdba.mp4"
      autoplay
      muted
      playsinline
      preload="metadata"
      width="1920"
      height="1080"
      onloadeddata="this.previousElementSibling.style.display='none';"
    ></video>
    <div class="overlay"></div>
  </div>

  <!-- Content -->
  <div class="content">
    <h1>
      Transform Your Space with
      <span class="accent">FAM Building Material</span>
    </h1>
    <p>
      High-quality materials and fixtures for home renovation. We provide everything you need to refashion your living spaces with style and durability.
    </p>
    <div class="button-group">
      <a href="#brands" class="primary-button">
        View Our Work
      </a>
      <a href="/services" class="secondary-button">
        Explore Products
      </a>
    </div>
  </div>
</section>

<style>
  section {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .background {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
  }

  .background-video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .background-placeholder {
    width: 100%;
    height: 100%;
    object-fit: cover;
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .overlay {
    position: absolute;
    inset: 0;
    background-color: rgba(0, 0, 0, 0.6);
  }

  .content {
    position: relative;
    z-index: 10;
    text-align: center;
    padding: 0 1.5rem;
    max-width: 64rem;
    margin: 0 auto;
  }

  h1 {
    font-size: clamp(2rem, 5vw, 3.75rem);
    font-weight: 700;
    color: white;
    margin-bottom: 1.5rem;
  }

  .accent {
    display: block;
    margin-top: 0.5rem;
    color: var(--accent-color, #ff6b6b);
  }

  p {
    font-size: clamp(1.125rem, 2vw, 1.25rem);
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 2rem;
    max-width: 42rem;
    margin-left: auto;
    margin-right: auto;
  }

  .button-group {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    justify-content: center;
  }

  @media (min-width: 640px) {
    .button-group {
      flex-direction: row;
    }
  }

  button, .primary-button, .secondary-button {
    padding: 0.75rem 2rem;
    border-radius: 0.375rem;
    font-weight: 600;
    transition: background-color 0.3s ease;
    text-decoration: none;
    border: none;
    cursor: pointer;
    display: inline-block;
  }

  .primary-button {
    background-color: var(--accent-color, #ff6b6b);
    color: white;
  }

  .primary-button:hover {
    background-color: color-mix(in srgb, var(--accent-color, #ff6b6b) 90%, black);
  }

  .secondary-button {
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: white;
  }

  .secondary-button:hover {
    background-color: rgba(255, 255, 255, 0.1);
  }
</style>