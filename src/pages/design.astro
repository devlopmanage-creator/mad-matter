---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
---


<Layout 
  title="Design Inspiration - FAMUAE | Modern Interior & Exterior Design Solutions"
  description="Discover design inspiration with FAMUAE's modern interior and exterior solutions. Explore our focus on indoor-outdoor living and contemporary design trends."
  keywords="design inspiration, modern interior design, exterior design, indoor outdoor living, contemporary design UAE, design trends, FAMUAE design"
>
  <Navigation />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/fullPage.js/4.0.24/fullpage.min.css" />
  <div id="fullpage">
    <section class="section design-hero collections-section">
      <!-- Simple CSS Carousel -->
      <div class="simple-carousel">
        <div class="carousel-container">
          <div class="carousel-slide active">
            <img src="https://res.cloudinary.com/doleg7smw/image/upload/v1759343593/23_fvxxy7.jpg" alt="Collection 1" class="design-img" loading="eager" />
          </div>
          <div class="carousel-slide">
            <img src="https://res.cloudinary.com/doleg7smw/image/upload/v1759343560/21_tucbvi.jpg" alt="Collection 2" class="design-img" loading="lazy" />
          </div>
          <div class="carousel-slide">
            <img src="https://res.cloudinary.com/doleg7smw/image/upload/v1759343560/20_ggtfms.jpg" alt="Collection 3" class="design-img" loading="lazy" />
          </div>
          <div class="carousel-slide">
            <img src="https://res.cloudinary.com/doleg7smw/image/upload/v1759343559/16_pp0gc8.jpg" alt="Collection 4" class="design-img" loading="lazy" />
          </div>
          <div class="carousel-slide">
            <img src="https://res.cloudinary.com/doleg7smw/image/upload/v1759343559/19_yldl5w.jpg" alt="Collection 5" class="design-img" loading="lazy" />
          </div>
        </div>
        
      <!-- Navigation (hidden for collections) -->
      <div class="carousel-nav" style="display: none;">
        <button class="carousel-btn prev" data-direction="-1">‹</button>
        <button class="carousel-btn next" data-direction="1">›</button>
      </div>
      
      <!-- Dots (hidden for collections) -->
      <div class="carousel-dots" style="display: none;">
        <span class="dot active" data-slide="1"></span>
        <span class="dot" data-slide="2"></span>
        <span class="dot" data-slide="3"></span>
        <span class="dot" data-slide="4"></span>
        <span class="dot" data-slide="5"></span>
      </div>
      
      <!-- Play/Pause indicator (appears when paused) -->
      <div class="carousel-play-indicator" style="display: none;">
        <button class="play-btn" onclick="resumeCarousel()">▶️</button>
        <span class="play-text">Click to resume slideshow</span>
      </div>
      </div>
    </section>
    <section class="section design-card">
      <h1 class="design-title">RADAWAY</h1>
      <div class="design-subtitle">INDOOR &mdash; OUTDOOR</div>
    </section>
    <section class="section design-card gallery-section">
      <h2 class="design-title">Gallery</h2>
      <div class="design-subtitle">COLLECTION SHOWCASE</div>
      <div class="gallery-grid">
        <div class="gallery-item">
          <img src="https://res.cloudinary.com/doleg7smw/image/upload/v1759343593/23_fvxxy7.jpg" alt="Collection 1" class="gallery-img" />
        </div>
        <div class="gallery-item">
          <img src="https://res.cloudinary.com/doleg7smw/image/upload/v1759343560/21_tucbvi.jpg" alt="Collection 2" class="gallery-img" />
        </div>
        <div class="gallery-item">
          <img src="https://res.cloudinary.com/doleg7smw/image/upload/v1759343560/20_ggtfms.jpg" alt="Collection 3" class="gallery-img" />
        </div>
        <div class="gallery-item">
          <img src="https://res.cloudinary.com/doleg7smw/image/upload/v1759343559/16_pp0gc8.jpg" alt="Collection 4" class="gallery-img" />
        </div>
        <div class="gallery-item">
          <img src="https://res.cloudinary.com/doleg7smw/image/upload/v1759343559/19_yldl5w.jpg" alt="Collection 5" class="gallery-img" />
        </div>
        <div class="gallery-item">
          <img src="https://res.cloudinary.com/doleg7smw/image/upload/v1759343559/16_pp0gc8.jpg" alt="Collection 6" class="gallery-img" />
        </div>
      </div>
      <div class="design-subtitle gallery-link-container">
        <a href="/gallery" class="design-link">
          VIEW FULL GALLERY →
        </a>
      </div>
    </section>

    <section class="section design-hero inspiration-section">
      <div class="inspiration-content">
        <div class="inspiration-text">
          <h2 class="inspiration-title">Design Inspiration</h2>
          <p class="inspiration-description">
            Discover the perfect blend of indoor and outdoor living with our curated collection of modern design solutions. 
            From contemporary furniture to architectural elements, explore how we transform spaces into living experiences.
          </p>
          <div class="inspiration-cta">
            <a href="/services" class="cta-button">Explore Our Services</a>
            <a href="/about-us" class="cta-button secondary">Learn More</a>
          </div>
        </div>
        <div class="inspiration-image">
          <img src="https://images.unsplash.com/photo-1500534314209-a25ddb2bd429?auto=format&fit=crop&w=1600&q=80" alt="Design Inspiration" class="design-img" />
          <div class="image-overlay">
            <div class="overlay-content">
              <span class="overlay-text">Modern Living Spaces</span>
            </div>
          </div>
        </div>
      </div>
    </section>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/fullPage.js/4.0.24/fullpage.min.js" is:inline></script>
  <script is:inline>
    // Initialize fullPage.js when DOM is loaded
    document.addEventListener('DOMContentLoaded', () => {
      // Check if fullpage is available and element exists
      if (typeof fullpage !== 'undefined' && document.getElementById('fullpage')) {
        try {
          new fullpage('#fullpage', {
            autoScrolling: true,
            navigation: true,
            scrollHorizontally: false,
            fitToSection: true,
            anchors: ['collections', 'radaway', 'gallery-link','inspiration'],
            navigationTooltips: ['Collections', 'Radaway', 'Gallery Link','Inspiration'],
            showActiveTooltip: true,
            responsiveWidth: 900,
            credits: { enabled: false },
            // Optimizaciones para móviles
            scrollBar: false,
            easingcss3: 'ease',
            scrollingSpeed: 700,
            // Asegurar que las secciones ocupen toda la altura en móviles
            paddingTop: '0px',
            paddingBottom: '0px',
            afterRender: function() {
              // Always start at first section
              if (window.location.hash) {
                if (typeof fullpage_api !== 'undefined') {
                  fullpage_api.moveTo(1);
                }
                history.replaceState(null, '', window.location.pathname);
              }
              
              // Ajustar altura en móviles después del render
              if (window.innerWidth <= 768) {
                setTimeout(() => {
                  const sections = document.querySelectorAll('.section');
                  sections.forEach(section => {
                    section.style.height = '100vh';
                    section.style.minHeight = '100vh';
                  });
                }, 100);
              }
            }
          });
        } catch (error) {
          console.warn('FullPage.js initialization failed:', error);
        }
      }

      // Remove watermark after initialization
      setTimeout(() => {
        const watermarks = document.querySelectorAll('.fp-watermark');
        watermarks.forEach(el => el.remove());
      }, 1000);

      // Simple carousel functionality
      let slideIndex = 1;
      let autoPlayInterval;
      
      // Initialize carousel
      function initCarousel() {
        const slides = document.querySelectorAll('.carousel-slide');
        const dots = document.querySelectorAll('.dot');
        const prevBtn = document.querySelector('.carousel-btn.prev');
        const nextBtn = document.querySelector('.carousel-btn.next');
        
        // Preload images for better performance
        preloadImages();
        
        // Show first slide
        showSlide(1);
        
        // Add event listeners to navigation buttons
        if (prevBtn) {
          prevBtn.addEventListener('click', () => changeSlide(-1));
        }
        
        if (nextBtn) {
          nextBtn.addEventListener('click', () => changeSlide(1));
        }
        
        // Add event listeners to dots
        dots.forEach((dot, index) => {
          dot.addEventListener('click', () => currentSlide(index + 1));
        });
        
        
        // Start auto-play
        startAutoPlay();
      }
      
      function changeSlide(n) {
        const slides = document.querySelectorAll('.carousel-slide');
        slideIndex += n;
        
        if (slideIndex > slides.length) { 
          slideIndex = 1; 
        }
        if (slideIndex < 1) { 
          slideIndex = slides.length; 
        }
        
        showSlide(slideIndex);
        restartAutoPlay(); // Restart auto-play when user interacts
      }
      
      function currentSlide(n) {
        slideIndex = n;
        showSlide(slideIndex);
        restartAutoPlay(); // Restart auto-play when user interacts
      }
      
      function showSlide(n) {
        const slides = document.querySelectorAll('.carousel-slide');
        const dots = document.querySelectorAll('.dot');
        
        // Remove active class from all slides and dots
        slides.forEach(slide => slide.classList.remove('active'));
        dots.forEach(dot => dot.classList.remove('active'));
        
        // Add active class to current slide and dot
        if (slides[n - 1]) {
          slides[n - 1].classList.add('active');
        }
        if (dots[n - 1]) {
          dots[n - 1].classList.add('active');
        }
      }
      
      function startAutoPlay() {
        autoPlayInterval = setInterval(() => {
          changeSlide(1);
        }, 4000);
        // Hide play indicator when playing
        const playIndicator = document.querySelector('.carousel-play-indicator');
        if (playIndicator) {
          playIndicator.style.display = 'none';
        }
      }
      
      function stopAutoPlay() {
        if (autoPlayInterval) {
          clearInterval(autoPlayInterval);
        }
        // Show play indicator when paused
        const playIndicator = document.querySelector('.carousel-play-indicator');
        if (playIndicator) {
          playIndicator.style.display = 'flex';
        }
      }
      
      function restartAutoPlay() {
        stopAutoPlay();
        startAutoPlay();
      }
      
      // Preload images to prevent layout shifts
      function preloadImages() {
        const images = document.querySelectorAll('.carousel-slide .design-img');
        let loadedCount = 0;
        
        images.forEach((img, index) => {
          const imageLoader = new Image();
          imageLoader.onload = function() {
            loadedCount++;
            if (loadedCount === images.length) {
              console.log('All images preloaded successfully');
            }
          };
          imageLoader.onerror = function() {
            loadedCount++;
          };
          imageLoader.src = img.src;
        });
      }
      
      // Initialize carousel when DOM is ready
      initCarousel();
      
      // Initialize gallery modal functionality
      initGalleryModal();
    });
  </script>
  
  <script is:inline>
    // Global function to resume carousel
    function resumeCarousel() {
      if (typeof startAutoPlay === 'function') {
        startAutoPlay();
        console.log('Carousel resumed');
      }
    }
    
    // Gallery modal functionality
    function initGalleryModal() {
      const galleryItems = document.querySelectorAll('.gallery-item');
      
      galleryItems.forEach((item, index) => {
        item.addEventListener('click', function(e) {
          e.preventDefault();
          
          const img = this.querySelector('.gallery-img');
          if (!img) return;
          
          const imgSrc = img.src;
          const imgAlt = img.alt;
          
          openImageModal(imgSrc, imgAlt, index + 1);
        });
      });
    }
    
    function openImageModal(imgSrc, imgAlt, slideNumber) {
      // Remove any existing modal first
      const existingModal = document.querySelector('.image-modal');
      if (existingModal) {
        existingModal.remove();
      }
      
      // Create modal overlay
      const modal = document.createElement('div');
      modal.className = 'image-modal';
      modal.style.cssText = `
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.9);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 10000;
        cursor: pointer;
        backdrop-filter: blur(5px);
      `;
      
      modal.innerHTML = `
        <div class="modal-content" style="
          background: white;
          border-radius: 12px;
          max-width: 90vw;
          max-height: 90vh;
          display: flex;
          flex-direction: column;
          box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
          overflow: hidden;
          cursor: default;
        ">
          <div class="modal-header" style="
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 1.5rem;
            border-bottom: 1px solid #eee;
            background: #f8f8f8;
          ">
            <h3 style="margin: 0; font-family: 'Playfair Display', serif; font-size: 1.2rem; color: #222;">${imgAlt}</h3>
            <button class="close-btn" style="
              background: none;
              border: none;
              font-size: 1.5rem;
              cursor: pointer;
              color: #666;
              padding: 0;
              width: 30px;
              height: 30px;
              display: flex;
              align-items: center;
              justify-content: center;
              border-radius: 50%;
              transition: all 0.2s ease;
            ">&times;</button>
          </div>
          <div class="modal-body" style="
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
            background: #fafafa;
          ">
            <img src="${imgSrc}" alt="${imgAlt}" class="modal-image" style="
              max-width: 100%;
              max-height: 70vh;
              object-fit: contain;
              border-radius: 8px;
              box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            " />
          </div>
        </div>
      `;
      
      document.body.appendChild(modal);
      document.body.style.overflow = 'hidden';
      
      // Add event listeners
      const closeBtn = modal.querySelector('.close-btn');
      
      closeBtn.addEventListener('click', function(e) {
        e.stopPropagation();
        closeImageModal();
      });
      
      closeBtn.addEventListener('mouseenter', function() {
        this.style.background = '#e0e0e0';
        this.style.color = '#222';
      });
      
      closeBtn.addEventListener('mouseleave', function() {
        this.style.background = 'none';
        this.style.color = '#666';
      });
      
      // Close when clicking outside
      modal.addEventListener('click', function(e) {
        if (e.target === modal) {
          closeImageModal();
        }
      });
      
      // Close with Escape key
      const escapeHandler = function(e) {
        if (e.key === 'Escape') {
          closeImageModal();
          document.removeEventListener('keydown', escapeHandler);
        }
      };
      document.addEventListener('keydown', escapeHandler);
    }
    
    function closeImageModal() {
      const modal = document.querySelector('.image-modal');
      if (modal) {
        modal.remove();
        document.body.style.overflow = 'auto';
      }
    }
  </script>
  <!-- COMMENTED OUT FOOTER -->
  <!-- <Footer /> -->
</Layout>
  <style>
    .design-main {
      min-height: 100vh;
      width: 100vw;
      margin: 0;
      padding: 0;
    }
    .section {
      width: 100vw;
      height: 100vh;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      margin: 0;
      padding: 0;
      position: relative;
    }
    .design-img {
      width: 100vw;
      height: 100vh;
      min-height: 400px;
      object-fit: cover;
      margin: 0;
      display: block;
    }
    .design-card {
  width: 100vw;
  background: #fff;
  box-shadow: 0 2px 24px #0002;
  text-align: center;
  padding: 2.5rem 2rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: auto;
  min-height: 320px;
    }
    .design-title {
      font-family: 'Playfair Display', serif;
      font-size: 2.8rem;
      font-weight: 400;
      color: #222;
      margin-bottom: 1.2rem;
      letter-spacing: 1px;
    }
    .design-subtitle {
      font-family: 'Montserrat', Arial, sans-serif;
      font-size: 1.1rem;
      color: #222;
      letter-spacing: 2px;
      margin-bottom: 0;
      text-transform: uppercase;
      font-weight: 400;
    }
    .gallery-link-container {
      margin-top: 1.5rem;
    }
    
    .design-link {
      display: inline-block;
      color: #fff;
      background: #bfa16b;
      text-decoration: none;
      font-weight: 600;
      padding: 0.7rem 1.8rem;
      font-size: 0.95rem;
      letter-spacing: 1.5px;
      transition: all 0.3s ease;
      border: 2px solid #bfa16b;
      cursor: pointer;
      box-shadow: 0 3px 12px rgba(191, 161, 107, 0.25);
    }
    
    .design-link:hover {
      background: transparent;
      color: #bfa16b;
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(191, 161, 107, 0.35);
    }
    
    .design-link:active {
      transform: translateY(-1px);
    }
    
    /* Simple Carousel Styles */
    .collections-section {
      position: relative;
      overflow: hidden;
    }
    
    .simple-carousel {
      width: 100vw;
      height: 100vh;
      position: relative;
      overflow: hidden;
    }
    
    .carousel-container {
      width: 100%;
      height: 100%;
      position: relative;
      overflow: hidden;
    }
    
    .carousel-slide {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100vh;
      opacity: 0;
      transition: opacity 0.5s ease-in-out;
      overflow: hidden;
    }
    
    .carousel-slide.active {
      opacity: 1;
    }
    
    .carousel-slide .design-img {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
      display: block;
      margin: 0;
      padding: 0;
      transition: opacity 0.5s ease-in-out;
      background-color: #f0f0f0;
    }
    
    /* Preloader for images */
    .carousel-slide .design-img:not([src]) {
      background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
      background-size: 200% 100%;
      animation: loading 1.5s infinite;
    }
    
    @keyframes loading {
      0% {
        background-position: 200% 0;
      }
      100% {
        background-position: -200% 0;
      }
    }
    
    /* Navigation buttons */
    .carousel-nav {
      position: absolute;
      top: 50%;
      width: 100%;
      display: flex;
      justify-content: space-between;
      padding: 0 30px;
      z-index: 10;
      transform: translateY(-50%);
    }
    
    .carousel-btn {
      background: rgba(255, 255, 255, 0.15);
      border: 2px solid rgba(191, 161, 107, 0.3);
      color: #bfa16b;
      font-size: 24px;
      font-weight: bold;
      width: 50px;
      height: 50px;
      border-radius: 50%;
      cursor: pointer;
      backdrop-filter: blur(10px);
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      user-select: none;
      outline: none;
    }
    
    .carousel-btn:hover {
      background: rgba(191, 161, 107, 0.25);
      border-color: rgba(191, 161, 107, 0.6);
      transform: scale(1.1);
      color: #fff;
    }
    
    .carousel-btn:active {
      transform: scale(0.95);
    }
    
    /* Dots */
    .carousel-dots {
      position: absolute;
      bottom: 30px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      gap: 10px;
      z-index: 10;
    }
    
    .dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.5);
      cursor: pointer;
      transition: all 0.3s ease;
      border: 2px solid transparent;
    }
    
    .dot.active {
      background: #bfa16b;
      transform: scale(1.2);
      border-color: rgba(255, 255, 255, 0.3);
    }
    
    .dot:hover {
      background: rgba(191, 161, 107, 0.8);
      transform: scale(1.1);
    }
    
    
    
    /* Play/Pause indicator */
    .carousel-play-indicator {
      position: absolute;
      bottom: 50px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      align-items: center;
      gap: 10px;
      background: rgba(0, 0, 0, 0.7);
      padding: 10px 20px;
      border-radius: 25px;
      backdrop-filter: blur(10px);
      z-index: 10;
    }
    
    .play-btn {
      background: none;
      border: none;
      font-size: 1.5rem;
      cursor: pointer;
      transition: transform 0.2s ease;
    }
    
    .play-btn:hover {
      transform: scale(1.1);
    }
    
    .play-text {
      color: white;
      font-size: 0.9rem;
      font-family: 'Montserrat', Arial, sans-serif;
    }
    
    
    /* Responsive Styles */
    @media (max-width: 900px) {
      .design-title { font-size: 2rem; }
      .design-card { padding: 1.5rem 0.5rem 1.2rem 0.5rem; }
      
      .design-link {
        padding: 0.65rem 1.5rem;
        font-size: 0.9rem;
      }
      
      .carousel-btn {
        width: 40px;
        height: 40px;
        font-size: 18px;
      }
      
      .carousel-nav {
        padding: 0 15px;
      }
      
      .carousel-dots {
        bottom: 20px;
      }
      
      .dot {
        width: 10px;
        height: 10px;
      }
    }
    
    @media (max-width: 600px) {
      /* Asegurar que no haya espacio blanco en móviles */
      body, html {
        height: 100%;
        overflow-x: hidden;
      }
      
      #fullpage {
        height: 100vh;
        min-height: 100vh;
      }
      
      .section {
        height: 100vh !important;
        min-height: 100vh !important;
        max-height: 100vh;
      }
      
      .design-title { font-size: 1.5rem; }
      .design-subtitle { font-size: 0.9rem; }
      
      .design-link { 
        font-size: 0.85rem;
        padding: 0.6rem 1.3rem;
        letter-spacing: 1.2px;
      }
      
      .gallery-link-container {
        margin-top: 1.2rem;
      }
      
      .design-card { padding: 1rem 0.5rem 0.8rem 0.5rem; min-height: 240px; }
      
      .carousel-btn {
        width: 35px;
        height: 35px;
        font-size: 16px;
        display: none; /* Hide navigation arrows on very small screens */
      }
      
      .dot {
        width: 8px;
        height: 8px;
      }
      
      .gallery-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
        margin: 1.5rem 0;
        max-width: 600px;
      }
      
      .gallery-img {
        height: 150px;
      }
    }
    
    @media (max-width: 480px) {
      /* Play indicator responsive */
      .carousel-play-indicator {
        bottom: 30px;
        padding: 8px 15px;
      }
      
      .play-text {
        font-size: 0.8rem;
      }
      
      .gallery-grid {
        grid-template-columns: 1fr;
        gap: 1rem;
        margin: 1rem 0;
        max-width: 300px;
      }
      
      .gallery-img {
        height: 200px;
      }
    }

    /* Gallery Section Styles */
    .gallery-section {
      background: #fff;
      padding: 2rem;
      overflow: hidden;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }
    
    .gallery-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
      margin: 2rem 0;
      max-width: 800px;
      width: 100%;
    }
    
    .gallery-item {
      position: relative;
      overflow: hidden;
      border-radius: 8px;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      cursor: pointer;
      background: #f8f8f8;
    }
    
    .gallery-item:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
    }
    
    .gallery-item:hover::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(191, 161, 107, 0.1);
      z-index: 1;
    }
    
    .gallery-img {
      width: 100%;
      height: 180px;
      object-fit: cover;
      display: block;
      transition: transform 0.3s ease;
    }
    
    .gallery-item:hover .gallery-img {
      transform: scale(1.05);
    }

    /* Inspiration Section Styles */
    .inspiration-section {
      background: linear-gradient(135deg, #f8f8f8 0%, #e8e8e8 100%);
      padding: 0;
      overflow: hidden;
    }
    
    .inspiration-content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      height: 100vh;
      width: 100vw;
    }
    
    .inspiration-text {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: flex-start;
      padding: 4rem 3rem;
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      position: relative;
      z-index: 2;
    }
    
    .inspiration-title {
      font-family: 'Playfair Display', serif;
      font-size: 3rem;
      font-weight: 400;
      color: #222;
      margin-bottom: 1.5rem;
      letter-spacing: 1px;
      line-height: 1.2;
    }
    
    .inspiration-description {
      font-family: 'Montserrat', Arial, sans-serif;
      font-size: 1.1rem;
      color: #555;
      line-height: 1.6;
      margin-bottom: 2.5rem;
      max-width: 500px;
    }
    
    .inspiration-cta {
      display: flex;
      gap: 1.5rem;
      flex-wrap: wrap;
    }
    
    .cta-button {
      padding: 1rem 2rem;
      border: 2px solid #bfa16b;
      background: #bfa16b;
      color: white;
      text-decoration: none;
      font-family: 'Montserrat', Arial, sans-serif;
      font-size: 1rem;
      font-weight: 500;
      letter-spacing: 1px;
      text-transform: uppercase;
      border-radius: 0;
      transition: all 0.3s ease;
      display: inline-block;
      text-align: center;
      min-width: 180px;
    }
    
    .cta-button:hover {
      background: transparent;
      color: #bfa16b;
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(191, 161, 107, 0.3);
    }
    
    .cta-button.secondary {
      background: transparent;
      color: #bfa16b;
    }
    
    .cta-button.secondary:hover {
      background: #bfa16b;
      color: white;
    }
    
    .inspiration-image {
      position: relative;
      overflow: hidden;
      height: 100vh;
    }
    
    .inspiration-image .design-img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
      transition: transform 0.5s ease;
    }
    
    .image-overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(45deg, rgba(191, 161, 107, 0.1) 0%, rgba(0, 0, 0, 0.1) 100%);
      display: flex;
      align-items: flex-end;
      justify-content: flex-start;
      padding: 3rem;
      opacity: 0;
      transition: opacity 0.3s ease;
    }
    
    .inspiration-image:hover .image-overlay {
      opacity: 1;
    }
    
    .inspiration-image:hover .design-img {
      transform: scale(1.05);
    }
    
    .overlay-content {
      background: rgba(255, 255, 255, 0.9);
      padding: 1.5rem 2rem;
      border-radius: 8px;
      backdrop-filter: blur(10px);
      border: 1px solid rgba(191, 161, 107, 0.2);
    }
    
    .overlay-text {
      font-family: 'Playfair Display', serif;
      font-size: 1.5rem;
      color: #222;
      font-weight: 400;
      letter-spacing: 1px;
    }
    
    /* Inspiration Section Responsive */
    @media (max-width: 900px) {
      .inspiration-content {
        grid-template-columns: 1fr;
        grid-template-rows: auto 1fr;
      }
      
      .inspiration-text {
        padding: 2rem 1.5rem;
        min-height: 40vh;
      }
      
      .inspiration-title {
        font-size: 2.2rem;
      }
      
      .inspiration-description {
        font-size: 1rem;
        margin-bottom: 2rem;
      }
      
      .inspiration-cta {
        gap: 1rem;
      }
      
      .cta-button {
        padding: 0.8rem 1.5rem;
        font-size: 0.9rem;
        min-width: 150px;
      }
      
      .inspiration-image {
        height: 60vh;
        min-height: 300px;
      }
      
      .image-overlay {
        padding: 2rem 1.5rem;
      }
      
      .overlay-content {
        padding: 1rem 1.5rem;
      }
      
      .overlay-text {
        font-size: 1.2rem;
      }
    }
    
    @media (max-width: 600px) {
      .inspiration-content {
        height: 100vh;
        min-height: 100vh;
      }
      
      .inspiration-text {
        padding: 1.5rem 1rem;
        min-height: 45vh;
      }
      
      .inspiration-title {
        font-size: 1.8rem;
        margin-bottom: 1rem;
      }
      
      .inspiration-description {
        font-size: 0.9rem;
        margin-bottom: 1.5rem;
      }
      
      .inspiration-cta {
        flex-direction: column;
        gap: 0.8rem;
      }
      
      .cta-button {
        padding: 0.7rem 1.2rem;
        font-size: 0.85rem;
        min-width: 140px;
      }
      
      .inspiration-image {
        height: 55vh;
        min-height: 55vh;
        flex: 1;
      }
      
      .image-overlay {
        padding: 1.5rem 1rem;
      }
      
      .overlay-content {
        padding: 0.8rem 1.2rem;
      }
      
      .overlay-text {
        font-size: 1rem;
      }
    }

  </style>
</Layout>